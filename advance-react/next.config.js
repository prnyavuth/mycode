// next.config.js
module.exports = {
  reactStrictMode: true, // Enable strict mode for React, helping to identify potential problems in the app
  swcMinify: true, // Enable SWC-based minification for faster builds and smaller bundle sizes
}

